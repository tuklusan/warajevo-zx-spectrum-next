<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0348 Preflight: Retire Inactive Local Lab Machines

Status: APPROVED_FOR_IMPLEMENTATION

## Functional Inventory

Retire `windows-11-laptop` and `macos-bigsur-lab` from the active local remote
harness registry without modifying either physical machine. The active local
registry retains `linux-x64-lxqt` and `windows-10-reference`. The hosted
platform-smoke workflow remains the separate mandatory 20-lane topology and
must remain byte-for-byte unchanged.

Functional aspects covered: active registry membership, unknown-target failure,
remaining-host identity preservation, current operational documentation,
validator fixtures, historical evidence classification, hosted lane identity,
publication expected-lane identity, and private evidence preservation.

## Upstream Discovery

The complete repository search covers the retired names, their addresses,
machine counts, active registry and command examples, workflow validators,
tests, preflight records, issue records, and remote evidence paths.

- Active operational references: registry, `WORKFLOW.md`,
  `README-GIT-GITHUB.md`, `test-artefacts/README.md`,
  `tools/harness/README.md`, and active validator expectations. These are
  reconciled in this CR.
- Historical references: closed CR preflights, issue notes, and retained
  evidence paths describing executions that actually occurred. These remain
  unchanged so closure evidence stays truthful.
- Hosted references: `.github/workflows/platform-smoke.yml` and its 20-lane
  publication expected set. These are protected invariants and are not edited.

## Disposition

- Active registry entries for the two retired hosts: `IMPLEMENT_NOW` remove.
- Remaining Linux and Windows 10 entries: `PRESERVE` unchanged.
- Hosted workflow, lane labels, and publication expected set: `PRESERVE`
  byte-for-byte and semantically unchanged.
- Historical records and retained evidence: `PRESERVE` unchanged.

## Zero-Gap Exit Scan

The exit scan must show that the two retired names are absent from active
registry and current operational commands, unknown-target invocation fails
normally, Linux and Windows 10 entries are unchanged, historical records are
preserved, the workflow blob and SHA-256 are unchanged, all 20 hosted lane IDs
and labels are unchanged, the publication expected set is unchanged, and no
hosted validation requirement was weakened. Executable harness regression is
performed only on the authorized remote Linux machine; no local product build
or test is permitted.
