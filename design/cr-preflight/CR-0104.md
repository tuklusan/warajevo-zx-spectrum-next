<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0104 Preflight: Overscan and Rainbow Timing Transitions

Status: CLOSED

## Objective

Exercise and preserve border-to-active-display and active-to-border transitions
from canonical ULA timing and bus events, without software-recognition hacks.

## Functional Inventory

- Define transition fixtures around the visible-area boundaries.
- Derive every transition from emulated master ticks and ULA fetch timing.
- Preserve border color and active-pixel samples at adjacent edges.
- Cover overscan/rainbow-style timing without host presentation dependencies.
- Produce deterministic event evidence for repeated runs.

## Upstream Completeness Gate

Rescan the preserved reference border, raster, ULA-fetch, and floating-bus paths
and the architecture timing tables. Account for each boundary transition,
contention ordering, and invalid timing input; assign any omitted behavior to a
named later task rather than silently approximating it.

## Upstream Discovery

The reference behavior is expressed through timed display and bus activity.
Existing core work provides border events, ULA fetch scheduling, raster samples,
and floating-bus observation; this CR adds transition-level regression evidence.

## Disposition

Add deterministic transition fixtures and trace assertions at the core timing
boundary. Keep software pattern recognition and host presentation outside the
timing model.

## Zero-Gap Exit Scan

Before closure, rescan all visible-area edges, border changes, active fetches,
contention ordering, floating-bus observations, frame wrap, and host boundaries.
Confirm no scoped overscan/rainbow rule remains implicit.

## Verification Plan

- Local static gates and source scan only; no local build or test.
- CODE and DOCUMENTATION advisory reviews against this preflight.
- Remote Linux build/test with transition fixtures and complete corpus.
- Hosted platform smoke across all supported labels.
- Compare deterministic event traces across supported compilers and hosts.

## Closure Evidence

- CODE and DOCUMENTATION reviews: PASS with no confirmed findings.
- Linux remote: `CR0104-OVERSCAN-LINUX-R3`, all five CTest targets and the
  complete pinned corpus passed; pull-back completed successfully.
- Hosted validation: `33590125872`, all 18 available runner labels passed.
- Final upstream zero-gap scan: passed; later multicolor and hash/diagnostic
  tasks remain explicitly ordered in the architecture backlog.
