<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0271 Freeze Noninteractive Intel Mac SSH Fallback

Status: APPROVED_FOR_IMPLEMENTATION
Priority: HIGH
Execution order: WORKFLOW_FIRST_OUT_OF_ORDER
Blocking: NO

## Functional Inventory

- Use the proven password-authenticated SSH path for the Intel Mac when its
  local secret environment variable is present and the password helper exists.
- Force password-only authentication, a TTY, bounded connect timeout, and
  noninteractive host-key handling so no prompt can stall the harness.
- Preserve the configured private-key fallback when the local password secret
  is absent; never embed a credential in tracked source or evidence payloads.
- Keep the Mac project path and one-gigabyte usage guard unchanged.

## Upstream Discovery

- Compare the manual proven SSH invocation with the frozen
  `invoke_remote_harness.py` path and inspect all call sites for Mac sync,
  command execution, and artifact pull.
- Confirm the correction changes authentication orchestration only and does not
  change build, test, screenshot, cleanup, or evidence semantics.
- Perform a second call-site scan after implementation and verify no other Mac
  SSH path can silently fall back to an interactive prompt.

## Required Evidence

- Exact-SHA CODE and DOCUMENTATION reviews.
- A published Mac probe or smoke run producing a session manifest, followed by
  the normal remote evidence inspection and hosted validation.
- No local build or test activity.

## Exit Gates

Close only after the noninteractive password path is proven, key fallback is
preserved, the second call-site scan is clean, serial reviews pass, and the
publication gate succeeds.
