# Warajevo ZX Spectrum Next
# Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
# New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
# Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
# See LICENSE.txt and NOTICE.md for complete terms and provenance.

## Change Request
CR-0303: Harden hosted publication-lane verification

Status: CLOSED
Priority: high

## Functional Inventory
- Validate exactly the 20 expected product lane jobs and no housekeeping/publication job.
- Validate every lane head SHA equals the matrix commit.
- Validate every lane has a terminal successful conclusion.
- Require at least one successful Intel macOS lane and one successful ARM macOS lane.
- Make validation independent of API job ordering and shell tab/newline parsing quirks.
- Emit actionable diagnostics when the publication check fails.
- Reuse the completed all-green product matrix when only the publication instrumentation changes.
- Do not alter product build/test execution or frozen local/remote harnesses.

## Upstream Discovery
The hosted publication gate is project-owned workflow infrastructure. The
current run `34160498964` produced 20 successful product lanes but the
line-oriented publication check failed, so its result is a false negative.
The required matrix lanes and macOS acceptance rule are defined in
`.github/workflows/platform-smoke.yml` and `WORKFLOW.md`. No upstream legacy
behavior applies; the gate itself is the authority for this workflow invariant.

## Disposition
`IMPLEMENT_NOW`: replace brittle shell membership/row parsing with explicit
normalized lane-name, SHA, and conclusion sets and diagnostic output. This is
post-matrix instrumentation only; the completed run `34160498964` remains the
product evidence and must not be rerun solely for this correction.

## Zero-Gap Exit Scan
Confirm exact 20-lane cardinality, exact expected-name set, exact commit SHA,
successful terminal conclusions, Intel/ARM macOS coverage, and diagnostics for
each rejection path.

## Exit Gates
- CODE review passes with no unresolved findings.
- Reuse the complete passing matrix evidence; no product matrix rerun for this instrumentation-only correction.
- Hosted workflow validation of the corrected publication checker passes.
- Retained evidence and the reused matrix manifest tree are inspected.
- TEST_ARTIFACT review passes against retained evidence.
- Closure evidence is recorded while this CR remains active.
- Final DOCUMENTATION review passed with zero confirmed findings; tracker and preflight are closed.

## Validation Evidence
Workflow correction commit: `cc6c7f2fbf36d431b7859a3c96e28b643505249c`.

The original product matrix run `34160498964` had all 20 product lanes
successful but its publication checker returned a false negative. Its run
and uploaded bundles were subsequently removed by deep housekeeping before
they could be downloaded for this CR. The corrected checker was validated by
the automatically triggered run `34161538129`, which completed successfully
with all 20 lanes and publication verification green. This later run was not
used to justify a product change; it validated the workflow instrumentation.

The corrected gate normalizes the API response as JSON, compares the exact
20-lane set, verifies every lane SHA and successful conclusion, requires Intel
and ARM macOS coverage, and prints normalized lane diagnostics on failure.
The validation evidence tree under
`test-artefacts/hosted-runs/CR-0303-validation` passed
`verify_platform_evidence.py --verify-tree`.

The representative Windows ARM manifest passed TEST_ARTIFACT review with zero
confirmed findings; packet manifest hash:
`51ea82657bab556d68d8b1f4ce80fa3f677068080a729dc54e16a9789b93001b`.
