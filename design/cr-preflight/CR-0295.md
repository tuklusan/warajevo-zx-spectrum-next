<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0295: Snapshot Inspector Metadata Display

Status: CLOSED

## Functional Inventory

- Present the live inspector's format and version provenance.
- Present the implied machine profile, CPU register summary, paging state, AY register state, memory-page inventory, and warning count.
- Keep the projection read-only, bounded, deterministic, and suitable for a later native host view.

## Upstream And Architecture Discovery

The UI architecture sections 13.1-13.3 require a native Snapshot Inspector boundary and list format/version, model, registers, paging, AY state, pages, and warnings as applicable. The core already owns machine profile, debugger snapshot/page projection, AY accessors, and 48K/128K memory ownership. The inspector therefore projects those shared seams and does not parse or duplicate emulator state.

## Upstream Discovery

The upstream reference was checked for the corresponding debugger/state ownership boundaries. No upstream UI metadata contract was found that supersedes the project's existing debugger snapshot seam; no undispositioned upstream behavior is introduced by this live-machine-only display step. File-format-specific parsing remains a later task with its own authority and tests.

## Disposition

Implement a copied metadata view in `wz_snapshot_inspector_t` and a bounded text formatter. Runtime/live provenance is explicit; warnings are zero for a valid live machine, while future file loaders may populate applicable warnings without changing this contract.

## Zero-Gap Exit Scan

Before closure, rescan the architecture task and upstream reference for each listed display aspect, verify every aspect has a field or explicit not-applicable disposition, and record the result in closure evidence.

## Exit Gates

- Exact CODE review passes for the active CR and candidate SHA.
- Hosted matrix build/test and evidence verification pass.
- TEST_ARTIFACT review passes for the retained manifest.
- Final DOCUMENTATION review passes while this CR remains active.
- Only then close the tracker record and publish.

## Validation Evidence

Candidate `939346b5e9a02dc75f262faad67087b94c9085c8` passed the exact CODE review with no findings. Hosted `platform-smoke` run `34124009650` completed successfully across all 20 configured lanes; the retained evidence tree passed `verify_platform_evidence.py`. The test-artifact review passed with no findings for the retained Linux result manifest. The hosted lanes are headless for this change and their supported Sokol visual step was skipped, so no screenshot artifact was applicable; all available manifests, logs, and evidence-inspection records were retained for closure. The final documentation review passed with no findings. The zero-gap exit scan confirms every CR-listed display category is represented by a bounded field and formatter output, with file-format-specific parsing explicitly deferred to later scoped work.
