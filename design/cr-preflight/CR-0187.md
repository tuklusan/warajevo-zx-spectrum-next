<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0187 Hosted Publication Gate Job Classification

Status: CLOSED

## Functional Inventory

- Count exactly the 18 configured platform matrix jobs in the publication gate.
- Exclude the dedicated `deep-housekeeping` job and the publication gate itself
  from the platform-job query.
- Continue requiring every expected platform name, exact published SHA, and a
  successful conclusion for each platform job.
- Continue requiring at least one successful Intel macOS job and one successful
  ARM macOS job.
- Preserve stale-run and temporary-artifact cleanup before all matrix jobs.

## Upstream Completeness Gate

This change concerns the project-owned hosted validation workflow; the
preserved upstream application has no equivalent hosted runner publication
gate. Compare the complete current workflow job graph and every publication
query predicate so no platform or cleanup behavior is accidentally omitted.

## Upstream Discovery

The current workflow has one housekeeping job, 18 platform matrix jobs, and
one publication gate. The publication query excluded only itself, therefore
the housekeeping job made the count 19 even though all 18 platform jobs were
successful. The corrected predicate excludes both non-platform jobs.

## Disposition

- Publication job classification predicate: `IMPLEMENT_NOW`.
- Matrix platform coverage and macOS acceptance rules: `PRESERVE`.
- Application behavior: `NOT_APPLICABLE`.

## Zero-Gap Exit Scan

Rescan the workflow job graph, matrix identifiers, cleanup dependency, query
predicate, count assertion, SHA assertion, and macOS success checks. The exit
scan is complete only when the hosted run proves exactly 18 platform jobs and
the publication gate passes on the same commit.

## Closure Requirements

- Static repository gates pass; no local build or test is run.
- Exact-SHA CODE and DOCUMENTATION advisory reviews pass with zero findings.
- Hosted run `33908356927` passed all 18 platform jobs, deep housekeeping, and
  the corrected publication gate on commit `a8bd7e0`.
- The change is pushed and recorded in the CR tracker.
