<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0293 Resilient pinned conformance-source acquisition

Status: APPROVED_FOR_IMPLEMENTATION
Priority: high
Execution order: workflow_infrastructure_before_architecture_queue
Blocking: yes

## Functional Inventory

- Retry only transient failure while acquiring the pinned conformance source.
- Use exponential backoff with a bounded maximum attempt count.
- Remove an incomplete private checkout before retrying.
- Preserve the exact repository authority, pinned revision, corpus selections,
  manifests, unresolved baseline, and resolved-revision fail-closed check.
- Surface the final acquisition failure as a lane failure; never manufacture
  test evidence or treat an unavailable source as a pass.

## Upstream And Architecture Discovery

- The hosted failure on run `34114714947` occurred in
  `tools/harness/run_fuse_ed_platform.py` during `git clone`/ref transport,
  before any product conformance command ran.
- Existing CR-0203 establishes the SourceForge repository and exact revision
  as the current upstream authority; this CR does not replace that authority.
- The correction is limited to transient acquisition recovery and does not
  change product code, corpus semantics, or locked local/hosted harnesses
beyond the concrete publication blocker.

## Upstream Discovery

The current SourceForge Fuse repository remains the upstream authority for the
existing pinned revision. The hosted failure log shows transport/ref-list
failures during source acquisition rather than a changed repository identity or
an invalid product result.

## Disposition

- Existing repository and pinned revision: `IMPLEMENT_NOW` and retain.
- Transient clone/fetch recovery: `IMPLEMENT_NOW` in the acquisition seam.
- Product, corpus, and result semantics: `NOT_APPLICABLE` to change.

## Zero-Gap Exit Scan

The acquisition entry point, its callers, the workflow invocation, the pinned
revision, and the retained baseline were checked together. No alternate source,
unverified revision, or evidence bypass is introduced.

## Exit Gates

Close only after exact CODE, hosted matrix completion with all configured lanes,
retained evidence inspection, TEST_ARTIFACT, final DOCUMENTATION, and
publication inspection pass. No local build or test is permitted.

## Validation Evidence

- Exact CODE review passed with zero confirmed findings at candidate
  `ae962d052f90151203cb85b27e93c0d9fba9ce26`, packet
  `c0b6f877769bbe6824d4ae224c7646660e68f40bb8aa10e5830d7b0251f32147`.
- Hosted `platform-smoke` run `34115851511` completed successfully across all
  20 configured lanes, including deep housekeeping and publication-gate.
- Retained hosted evidence under
  `test-artefacts/hosted-runs/CR-0293/` passed the platform evidence tree
  verifier.
- Focused TEST_ARTIFACT review passed with zero confirmed findings, packet
  `96020b784e98ab78e599b8006810ce4c87f1579a2f06cf616274cc30e999b2d0`.
