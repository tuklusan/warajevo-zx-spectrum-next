<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0253 Enforce Complete UDP-Only Forwarding for Traces and Logs

Status: CLOSED

## Functional Inventory

Follow up CR-0244 by proving that every approved project trace and diagnostic
log emission path is connected to one controlled forwarding boundary. Forward
records only over UDP to
`sanyalnet-oracle-vps2.duckdns.org:65514`, encoded as Unix syslog records.

- Inventory every trace producer, diagnostic log producer, dump utility, host
  callback, and application-facing diagnostic path.
- Classify each record as forwardable, local-only, or explicitly excluded.
- Route every forwardable class through `wz_syslog_udp`; no second network
  transport, TCP fallback, implicit broadcast, or direct socket path is allowed.
- Preserve the full binary circular trace as a private local artifact; never
  transmit machine state, ROM bytes, memory contents, host paths, credentials,
  or personal data.
- Use bounded RFC 5424-style Unix syslog records with deterministic facility,
  severity, source metadata, sanitization, and oversize behavior.
- Keep DNS resolution, socket activity, failure handling, and backpressure
  non-blocking and unable to alter canonical machine state or halt execution.
- Make enablement explicit and default-safe; no record may be sent before the
  forwarding boundary is intentionally enabled.

## Upstream Discovery

Before implementation approval, re-read the complete preserved diagnostic and
trace paths in the upstream reference and compare them with both architecture
documents. Record every source, mapping, exclusion, and failure rule in the
implementation audit. The zero-gap exit scan must show no unclassified trace
or log producer and no behavior silently bypassing the sole transport.

## Implementation Approval

The upstream/source inventory is recorded in
`design/trace-log-forwarding-inventory.md`. CR-0244 supplies the reviewed
wire-format and transport adapter; this CR is approved to add the shared
diagnostic forwarding boundary and its routing tests without changing the
canonical machine or timing model.

## Disposition

| Surface | Disposition | Reason |
|---|---|---|
| Structured trace fan-out | IMPLEMENT_NOW | Required complete forwarding boundary |
| Application diagnostic logs | IMPLEMENT_NOW | Required approved log route |
| Private binary trace files | LOCAL_ONLY | Sensitive and bounded local evidence |
| Test/harness output | LOCAL_ONLY | Evidence protocol, not product diagnostics |
| Alternate network transports | MUST_NOT_EXIST | UDP-only requirement |

## Zero-Gap Exit Scan

The preserved trace/debug references were scanned across `SPECSIM.ASM`,
`SPECINT.ASM`, `SPECMON.ASM`, and the named architecture trace sections. No
additional structured trace producer or approved application-log channel was
found beyond the paths listed in the inventory. The implementation must repeat
this scan after wiring and record any changed disposition.

## Required Evidence

- Static inventory showing all trace and diagnostic-log producers and their
  disposition.
- Contract tests for routing, Unix syslog framing, UDP-only behavior,
  sanitization, bounded packets, disabled mode, and failure isolation.
- Remote and hosted build/test results for applicable lanes.
- Inspected manifests, traces, and screenshots where supported, retained only
  in the private closure record under `test-artefacts`.
- Serial CODE, TEST_ARTIFACT, and DOCUMENTATION review packets for the exact
  implementation commit.

## Closure Record

- Final implementation commit: `d6e1fd996ee5c8485840673a7bfa32ab38816c45`.
- Linux `CR0253-LINUX-R1`, Mac Intel `CR0253-MAC-R1`, Windows 10
  `CR0253-WIN10-R1`, and Windows 11 `CR0253-WIN11-R2` completed successfully
  with retained artifacts. The Windows 11 sanitizer attempt is retained as an
  environmental limitation because its MinGW installation lacks sanitizer
  runtime libraries; the standard lane passed.
- Hosted run `34025215636` completed successfully: all 20 platform lanes,
  housekeeping, and publication passed. Twenty result manifests and evidence
  inspection records were retained; all 27,120 pinned cases passed with no
  silent skips, and every lane exercised the router contract test.
- Private evidence is retained under
  `test-artefacts/hosted-runs/CR0253-34025215636` and the remote-run evidence
  directories. The CODE review passed with zero findings; packet manifest:
  `6a7dcf02760579dd8e5bbe33cb2f312ae26932b4f9cf42056f8b6915f2a16303`.
- Serial TEST_ARTIFACT review passed with zero findings; packet manifest:
  `4e62b38e1b55081903b2059cfaf3db5fff5f76e4ec5849227ca1f19d011727a7`.
- Final DOCUMENTATION reviews passed with zero findings: preflight packet
  `920f167e6f10037fa71a7b09ffd2de886cf403d0b60bdf3c6c36305e46706283` and
  inventory packet
  `86e072cbf6aaeb415813352b00b40375cd3585e48f070d4fdb2c0367cb9ba40`.

## Exit Gates

CR-0253 may close only after the upstream zero-gap scan, exact-SHA reviews,
remote/hosted validation, evidence inspection, and publication gate all pass.
Until then, CR-0244 remains the closed record for the existing transport
adapter and CR-0253 remains an open integration/completeness item.
