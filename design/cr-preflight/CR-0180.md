<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0180 Deep Hosted-Runner Housekeeping Gate

Status: CLOSED

## Functional Inventory

- Preserve the current hosted workflow run and its exact commit identity.
- Identify older `platform-smoke` runs on the same ref.
- Cancel only older queued or in-progress runs; never classify the current
  run's queued jobs as failures.
- Remove temporary Actions artifacts owned by those older runs.
- Clear generated build, test, result, and temporary workspace residue before
  the matrix and again in every matrix lane.
- Preserve checked-in source, guidance, committed evidence, and the current
  run's artifacts.
- Fail closed when required GitHub identity or authorization is unavailable.
- Keep cleanup bounded to this repository and workflow.
- Retain `fail-fast: false` and the terminal-state publication gate.
- Keep cleanup auditable through the workflow log and a tracked script.

## Authority And Upstream Scan

This infrastructure-only CR is governed by `WORKFLOW.md`, the hosted workflow,
the harness contract, and `tools/validate_project_gates.py`. The upstream
reference repository was checked for product behavior and contains no hosted
CI policy that this project is migrating. The repository scan covered the
workflow concurrency policy, matrix and publication gate, artifact upload
paths, private-artifact rules, harness temporary directories, and all existing
runner-waiting instructions.

## Upstream Discovery

The upstream product source and architecture contain no runner queue or hosted
artifact behavior. The repository-side discovery covered every current
workflow, concurrency rule, artifact path, temporary path, and cleanup rule;
the infrastructure behavior is project-owned and does not replace upstream
emulator functionality.

## Disposition

Dispositions:

- Hosted queue cleanup: `IMPLEMENT_NOW`; project CI governance scope.
- Same-ref and same-workflow restriction: `IMPLEMENT_NOW`; prevents unrelated
  branch runs from being affected.
- Current-run preservation: `IMPLEMENT_NOW`; required by publication identity.
- Generated workspace cleanup: `IMPLEMENT_NOW`; required for deterministic
  runner reuse.
- Source, guidance, and retained-evidence preservation: `IMPLEMENT_NOW`;
  required by repository provenance and evidence rules.
- Product behavior: `NOT_APPLICABLE`; no emulator or upstream behavior changes.

## Zero-Gap Exit Scan

The second scan revisited the cleanup candidate list, workflow permissions,
same-ref filtering, run identity, artifact ownership, workspace paths,
failure behavior, and publication dependency. No undispositioned behavior
remains. The gate is limited to older same-ref workflow state and generated
workspace residue, while current-run waiting and terminal publication checks
remain unchanged.

## Closure Requirements

- Static repository gates pass.
- Exact-SHA CODE and DOCUMENTATION reviews pass.
- The hosted matrix completes all configured lanes and publication-gate.
- Cleanup logs show current-run preservation and bounded deletion scope.

Closed on 2026-09-04 after hosted run `33914657804` preserved the current run,
completed housekeeping, and passed the publication gate at the validated commit.
