<!-- Warajevo ZX Spectrum Next maintenance preflight -->

# CR-0352 Hosted CODE Receipt Transport

Status: APPROVED_FOR_IMPLEMENTATION
Review-Base: ac19989b94e641e75d2d71423caa079a4396f583
Operator-Authorized-Gate-Change: YES

## Scope

Provide the exact v4 CODE-PASS receipt to protected hosted runners through a
workflow input and materialize it before validation. Preserve HMAC authorization,
exact-head binding, all 20 lanes, and fail-closed receipt validation.

No product behavior, Architecture #3, working harness, wrapper, or protected
review semantics are in scope.

## Upstream Discovery

The current hosted workflow checks out a tree that intentionally excludes the
private CODE receipt, while remote validation requires that receipt. Tracking it
would make the receipt part of the snapshot it authorizes. The maintenance
change resolves this transport contradiction without changing validation logic.

## Zero-Gap Exit Scan

Authorization, receipt identity, HMAC, exact-head, runner materialization,
matrix cardinality, publication, and cleanup paths are covered. No open gap
remains within this maintenance scope.

## Exit Gates

Standalone bootstrap PASS, exact published maintenance ref, immutable bootstrap
receipt inspection, and normal CR-0344 review gates are required before closure.
