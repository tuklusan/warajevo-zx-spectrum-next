<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0203 Refresh Reachable Fuse Corpus Pin

Status: APPROVED_FOR_IMPLEMENTATION

## Functional Inventory

- Keep the existing Fuse corpus repository and complete selection contract.
- Replace the no-longer-reachable pinned revision with a reachable revision
  from the same upstream repository.
- Keep source acquisition private under the project-local test-artefacts tree.
- Keep resolved-revision verification fail-closed.
- Keep the corpus path, runner invocation, manifests, and unresolved baseline
  semantics unchanged.
- Record the new revision consistently in every current authority surface.

## Upstream Completeness

The requested behavior is dependency provenance rather than emulator behavior.
The existing SourceForge repository remains the upstream authority.

## Upstream Discovery

The upstream repository `https://git.code.sf.net/p/fuse-emulator/fuse` was
queried directly. The prior revision
`9cbab635f5c9dfdfc5cb769aa89048c7e624d6b7` is no longer advertised or
fetchable, while current `HEAD` resolves to
`e7fe9a0f3625b0aef649b114fc98323c7d241840` and contains `z80/tests`.
The current revision is therefore the candidate replacement, pending hosted
and local soak validation of the complete existing corpus harness.

## Disposition

- Unreachable old revision: `REIMPLEMENTED_DIFFERENTLY` as a reachable pin
  from the same upstream repository, with the reason recorded above.
- Current Fuse source URL: `IMPLEMENT_NOW` and retain.
- Corpus selection and test semantics: `IMPLEMENT_NOW` unchanged.
- Historical CR notes containing the former pin: `NOT_APPLICABLE` to rewrite;
  they remain immutable historical evidence.

## Zero-Gap Exit Scan

The current harness scripts, unresolved baseline, and Fuse acquisition document
were searched for the old pin and updated consistently. Historical tracker
records are intentionally retained as historical evidence and are not current
acquisition authorities.

## Evidence

Static gates and `git diff --check` are required locally. No local build or test
is run before review. After review, run the complete local soak set and the
complete hosted matrix, awaiting all hosted lanes and publication.
