<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0268 Freeze Reliable Intel Mac SSH Harness Authentication

Status: APPROVED_FOR_IMPLEMENTATION
Priority: LOW
Execution order: WORKFLOW_FIRST
Blocking: NO

## Functional Inventory

- Use the proven Intel Mac SSH session shape: TTY, explicit password
  authentication, and public-key authentication disabled.
- Read the password only from `WZSN_MAC_SSH_PASSWORD`; never store it in the
  repository or command arguments.
- Retain the configured private-key path as fallback when the password secret
  or `sshpass` is unavailable.
- Apply the same frozen path to smoke, screenshot, synchronization, and
  artifact-pull operations.
- Do not change product, runner, or remote-machine behavior beyond connection
  setup.

## Upstream Discovery

- Compare the existing shared remote harness and Mac machine contract.
- Reconcile the successful interactive probe with the previous stalled
  non-interactive invocation.
- Confirm the Mac project path and one-gigabyte limit remain unchanged.

## Disposition

| Surface | Disposition | Reason |
|---|---|---|
| Mac SSH authentication | IMPLEMENT_NOW | Freeze known-working path |
| Key fallback | PRESERVE | Keeps existing lab setup usable |
| Remote project directory | PRESERVE | Existing confinement rule |
| Product behavior | DO_NOT_CHANGE | Harness-only workflow correction |

## Required Evidence

- Exact-SHA CODE and DOCUMENTATION reviews.
- Static project gates.
- One non-destructive Mac SSH probe using the frozen path.
- No local build or test activity.

## Zero-Gap Exit Scan

The existing harness, machine contract, and successful interactive probe were
compared line-by-line. The only missing behavior was the Mac authentication
mode; no project-directory, size-limit, artifact, or product behavior gap was
found.

## Exit Gates

Close after the frozen path is reviewed, the probe succeeds, the repository
gates pass, and the implementation is committed and pushed.
