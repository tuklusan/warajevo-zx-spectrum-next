<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0249 Implement Minimum Breakpoint Support

Status: CLOSED

## Functional Inventory

- Provide a shared breakpoint API over the one live machine and existing
  scheduler/CPU, without a debugger-owned executor or second memory model.
- Install, replace, remove, and inspect a bounded set of address breakpoints
  with deterministic validation and duplicate handling.
- Preserve the original instruction byte and mapped-page context before any
  instrumentation; restore it exactly on removal or replacement.
- Detect a breakpoint at the correct fetch boundary, stop before executing the
  target instruction, and emit deterministic trace evidence.
- Define ROM, 48K/128K mapping, address-wrap, already-installed, capacity, and
  invalid-argument behavior without mutating canonical state on failure.
- Keep stepping, continue, conditional expressions, UI presentation, and
  breakpoint persistence outside this minimum API.

## Upstream Discovery

- Architecture 1 sections 41 and 44; UI Architecture 2 section 21.
- `design/debugger-api.md`, `design/monitor-debugger-inventory.md`, and task
  ledger task 230.
- `src/core/wz_debugger.h`, `src/core/wz_debugger.c`, `src/core/wz_machine.h`,
  `src/core/wz_machine.c`, `src/core/wz_z80.c`, and scheduler/bus boundaries.
- Preserved upstream `reference/original-warajevo/source/src/spectrum-kernel/SPECMON.ASM`,
  including `BREAKP`, `BREAK1`, `BPSUB*`, `DISPBP`, `DISPBPRAM`, `DISPBPPG`,
  `SSTOP*`, and monitor fetch/stop paths.

## Zero-Gap Exit Scan

The broader scan covers breakpoint lifecycle, duplicate/capacity behavior,
instruction-fetch timing, original-byte restoration, 48K/128K mapped-page
context, ROM protection, failure atomicity, trace emission, and neighboring
step/continue paths. No undispositioned behavior remains.

## Disposition

| Surface | Disposition | Reason |
|---|---|---|
| Minimum address breakpoint lifecycle | IMPLEMENT_NOW | Task 230 |
| Fetch-boundary stop and trace marker | IMPLEMENT_NOW | Required monitor behavior |
| Mapped-page and original-byte restoration | IMPLEMENT_NOW | Deterministic state safety |
| Conditional breakpoints | DEFERRED_WITH_CR | Later debugger expression layer |
| Single-step/continue | DEFERRED_WITH_CR | Task 231 |
| UI breakpoint panels and persistence | DEFERRED_WITH_CR | UI implementation queue |

## Exit Gates

- Static gates pass locally; no local build/test.
- Exact-SHA CODE, DOCUMENTATION, and TEST_ARTIFACT reviews pass serially.
- Four remote lanes and the complete hosted publication matrix pass.
- Breakpoint lifecycle and fetch-stop evidence are retained in the private CR
  closure record.

## Closure Evidence

- Final implementation commit: `b8ebb8888e9b284f19af9b65d778991e34102ada`.
- CODE, TEST_ARTIFACT, and DOCUMENTATION reviews passed serially with zero
  confirmed findings.
- Remote runs `CR0249-LINUX-R1`, `CR0249-MAC-R1`, `CR0249-WIN10-R1`, and
  `CR0249-WIN11-R1` passed; all manifests reported no missing tools.
- Hosted run `34014963955` reached `success`, with all configured lanes
  terminal and the publication gate passed.
- Private evidence is retained at
  `test-artefacts/reviewer/CR-0249-test-evidence.local.json`.
