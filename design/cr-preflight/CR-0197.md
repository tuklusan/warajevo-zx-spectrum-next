<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0197 Verify 128K Master-Tick, T-State, and Frame Relationships

Status: CLOSED

## Functional Inventory

- Verify the 128K profile converts master ticks to CPU T-state and phase using the frozen two-master-tick ratio.
- Verify one 128K raster line is 228 T-states and 456 raster clocks.
- Verify one 128K frame is 311 lines, 70,908 T-states, and 141,816 raster clocks.
- Verify exact line, frame, and wrap boundaries through the public raster-position API.
- Verify the 48K profile remains covered by the existing relationship tests.
- Do not change the canonical timing constants or duplicate raster logic in this verification CR.

## Upstream Completeness Gate

Compare the frozen 128K timing evidence, profile conversion helpers, raster-position
implementation, and existing boundary tests with the preserved reference timing paths.
This CR verifies relationships only; contention and other variants remain deferred.

## Upstream Discovery

The architecture and frozen timing evidence define the master-tick ratio, per-line
geometry, frame geometry, and wrap behavior. The implementation already centralizes
these conversions in the machine profile and raster-position API.

## Disposition

- Deterministic 128K relationship assertions: `IMPLEMENT_NOW`.
- 48K regression retention: `IMPLEMENT_NOW`.
- Contention, floating-bus refinements, and +2A/+3 differences: `DEFERRED`.

## Zero-Gap Exit Scan

Before closure, rescan upstream timing paths, frozen evidence, profile conversion
helpers, raster-position code, and tests. Confirm every base 128K relationship is
covered and no deferred behavior is silently included.

## Evidence and Exit Gates

- Exact-SHA CODE review and independent bootstrap review pass with zero findings.
- Static repository gates and `git diff --check` locally; no local build or test.
- Remote Linux sanitizer, Windows 10/11, Intel Mac, and hosted matrix validation pass.
- A second upstream zero-gap scan is recorded before closure.

## Closure Evidence

- Closed on 2026-09-04 after adding deterministic 128K master-tick, T-state, line,
  frame, and wrap-boundary assertions while leaving the timing engine unchanged.
- Exact-SHA CODE review and independent bootstrap review passed with zero findings for
  `4533ec7..218bcae`.
- Remote validation passed: `CR0197-LINUX-SAN-R1`, `CR0197-WIN10-R1`,
  `CR0197-WIN11-R1`, and `CR0197-MAC-R1`.
- Hosted `platform-smoke` run `33932832267` passed all 18 matrix lanes, deep
  housekeeping, and publication at `218bcae2bfd847647ed6fa0642d7bd9179913e0d`.
- Second upstream scan covered the preserved 128K timing paths, frozen timing
  evidence, profile conversion helpers, raster-position implementation, and tests;
  no undispositioned base-profile relationship was found.
