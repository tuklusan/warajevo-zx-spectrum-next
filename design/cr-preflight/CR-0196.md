<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0196 Implement 128K ULA Timing and Display-Bank Profile

Status: APPROVED_FOR_IMPLEMENTATION

## Functional Inventory

- Enable the frozen 128K PAL ULA fetch schedule through the shared profile-driven raster path.
- Preserve 228 T-states per line, 311 lines per frame, and 456 raster clocks per line.
- Fetch 192 display lines and 32 bitmap/attribute cells per line at the frozen start and interval.
- Select display RAM bank 5 or 7 from the 128K screen-select latch bit.
- Keep CPU-visible bank mapping and the 48K ULA path unchanged.
- Do not duplicate the raster engine or implement 128K contention in this CR.

## Upstream Completeness Gate

Compare the preserved 128K-conditioned machine paths and frozen timing evidence with
the profile, ULA fetch, and display-bank implementation. Defer contention and other
model variants to their ordered tasks.

## Upstream Discovery

The preserved reference contains 128K-specific machine paths, while the frozen
128K evidence defines the 228/311 geometry and the display-bank distinction. The
existing raster API already expresses fetch start, line count, cell count, interval,
and attribute offset as profile data.

## Disposition

- Base 128K profile geometry and ULA fetch schedule: `IMPLEMENT_NOW`.
- Screen bank 5/7 selection through the existing latch: `IMPLEMENT_NOW`.
- 128K contention, floating-bus refinements, and +2A/+3 differences: `DEFERRED`.

## Zero-Gap Exit Scan

Before closure, rescan upstream 128K paths, frozen timing evidence, profile fields,
shared ULA fetch code, display-bank mapping, and tests. Confirm no base-profile ULA
behavior is omitted and no deferred contention behavior is silently included.

## Evidence and Exit Gates

- Exact-SHA CODE review and independent bootstrap review pass with zero findings.
- Static repository gates and `git diff --check` locally; no local build or test.
- Remote Linux sanitizer, Windows 10/11, Intel Mac, and hosted matrix validation pass.
- A second upstream zero-gap scan is recorded before closure.
