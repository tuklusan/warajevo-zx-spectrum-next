# Warajevo ZX Spectrum Next
# Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
# New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
# Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
# See LICENSE.txt and NOTICE.md for complete terms and provenance.

## Change Request
CR-0325: Implement ZX Printer Manager presentation and export

Status: APPROVED_FOR_IMPLEMENTATION
Priority: medium

## Functional Inventory
- Present captured virtual ZX Printer output through a subordinate Media manager.
- Consume completed `wz_printer_flush_event_t` events and the shared bitmap exporter;
  do not access BIOS, LPT, host printer devices, or duplicate core printer semantics.
- Expose stable manager availability and disabled reasons when no captured output is
  available, while preserving the last accepted capture until explicitly replaced.
- Support explicit bitmap export into a caller-selected host destination through a
  host-side operation; failed or cancelled export must not mutate printer/core state.
- Keep printer mode, status, port behavior, and timing owned by the deterministic
  `wz_printer` peripheral and keep manager state outside canonical machine state.
- Register the manager and export operations through the shared command registry with
  the correct host-read/host-write permissions and no remote destructive behavior.
- Make the manager reachable as `Media > ZX Printer > Printer Manager...` and retain
  `Settings > Peripherals > ZX Printer` as a separate host presentation setting.

## Upstream Discovery
- `design/warajevo-zx-spectrum-next-ui-architecture.md` sections 7.1, 16, 24,
  37, 40, 45, 46, 47, 48, and 51.
- `design/warajevo-zx-spectrum-next-architecture.md` sections 13, 28, 34, and 49.1.
- `design/wzsn-architectures-1-2-developer-tasks.md` task 321.
- `src/core/wz_printer.h` and `src/core/wz_printer.c` define the preserved
  deterministic event/status/port boundary.
- `src/app/wz_printer_export.h` and `src/app/wz_printer_export.c` define the shared
  host bitmap projection boundary.
- `src/app/wz_command_registry.h` and `src/app/wz_command_registry.c` define the
  shared GUI/Telnet/test command boundary.

## Disposition
`IMPLEMENT_NOW`: add a project-owned printer manager state and command-facing
presentation/export seam over the existing core event and bitmap-export APIs. Do not
change printer emulation behavior or modify frozen harnesses.

## Zero-Gap Exit Scan
Re-read the cited UI and core sections after implementation. Confirm manager reachability,
event consumption, mode/bitmap fidelity, no host-device access, explicit output selection,
cancel/error state preservation, registry identity/permission/availability, keyboard path,
and remote-policy behavior are represented by tests and closure evidence.

## Exit Gates
- CODE review and bootstrap authorization pass for the exact implementation snapshot.
- Four serial remote lanes and one exact hosted matrix run pass or retain documented
  platform diagnostics.
- Retained manifests, logs, traces, and applicable visual evidence are inspected.
- TEST_ARTIFACT review passes against retained evidence.
- Closure evidence is recorded while this CR remains active.
- Final DOCUMENTATION review passes, then tracker and preflight close.

## Validation Evidence
Pending implementation and required gates.
