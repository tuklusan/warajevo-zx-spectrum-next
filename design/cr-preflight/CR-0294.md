<!--
Warajevo ZX Spectrum Next
Copyright (c) 2026 Supratim Sanyal, SANYALnet Labs, for new original project material.
New original material is licensed under GNU GPL v2 or later (GPL-2.0-or-later), as stated in LICENSE.txt.
Upstream Warajevo and third-party material retain their applicable copyrights and licenses.
See LICENSE.txt and NOTICE.md for complete terms and provenance.
-->

# CR-0294 Snapshot Inspector boundary

Status: APPROVED_FOR_IMPLEMENTATION
Priority: high
Execution order: architecture_2_phase_12p
Blocking: no

## Functional Inventory

- Expose the stable `tools.snapshot_inspector` application action identity.
- Open a read-only inspector view from the one live machine state.
- Reuse shared debugger snapshot and paging inspection machinery.
- Provide explicit open, close, and unavailable-state semantics.
- Keep UI presentation, file I/O, format/version decoding, AY detail, and
  state editing outside this task for their designated later tasks.
- Do not clone execution state or mutate the machine while inspecting.

## Upstream And Architecture Discovery

- UI Architecture Section 13.1 requires `Tools > Snapshot Inspector...` and
  stable action ID `tools.snapshot_inspector`.
- UI Architecture Sections 13.2 and 13.3 require inspection to reuse shared
  debugger/state machinery; detailed format/version and editable state surfaces
  are later task boundaries.
- Existing `wz_debugger_snapshot` and `wz_debugger_read_page_info` are the
  authoritative read-only seams; no parallel snapshot implementation is added.
- Upstream `SPECMON.ASM` and `WARAJEVO.PAS` establish the legacy inspection
  context; this task preserves the boundary without reproducing host chrome.

## Upstream Discovery

The preserved monitor and environment sources were scanned for snapshot
inspection entry behavior and state categories. The implementation reuses the
current debugger snapshot/page APIs; format-specific presentation and editing
remain explicitly deferred to tasks 291, 292, and 299.

## Disposition

- Stable action identity and shared read-only open view: `IMPLEMENT_NOW`.
- Format/version, AY, memory-page, warning presentation: `LATER` task 291.
- Snapshot state editing: `LATER` task 292 and shared debugger machinery.
- Native dialogs, file I/O, and GUI drawing: `LATER` designated UI tasks.

## Zero-Gap Exit Scan

The task entry, UI architecture sections, preserved monitor/environment
references, existing debugger APIs, command identity, and regression boundary
were checked together. No format decoding, state editor, host I/O, or duplicate
debugger implementation is silently introduced.

## Exit Gates

Close only after exact CODE, hosted matrix completion, retained evidence
inspection, TEST_ARTIFACT, final DOCUMENTATION, and publication inspection
pass. No local build or test is permitted.

## Validation Evidence
