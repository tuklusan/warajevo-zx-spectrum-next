name: CodeQL Deep One-Time Audit

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Deep CodeQL Analysis / ${{ matrix.language }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        language: [cpp, python]

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Initialize CodeQL engine
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: security-extended,security-and-quality

      - name: Build C/C++ database with CMake
        if: matrix.language == 'cpp'
        shell: bash
        run: |
          cmake -S src/cmake -B build/codeql \
            -DCMAKE_BUILD_TYPE=Release \
            -DWZSN_BUILD_SOKOL_HOST=OFF
          cmake --build build/codeql --parallel 2

      - name: Run deep analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: /language:${{ matrix.language }}
