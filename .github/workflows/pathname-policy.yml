name: Repository pathname policy

on:
  # Audit every direct commit after it reaches the project's main branch.
  push:
    branches: [main]
  # Manual runs are accepted only on main; the job enforces the ref and event.
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pathname-policy:
    name: Pathname policy
    runs-on: ubuntu-latest
    steps:
      - name: Require a main-branch push or dispatch
        shell: bash
        run: |
          if [[ "$GITHUB_REF" != "refs/heads/main" || ( "$GITHUB_EVENT_NAME" != "push" && "$GITHUB_EVENT_NAME" != "workflow_dispatch" ) ]]; then
            echo "Pathname policy runs only for direct pushes to main or manual dispatch on main." >&2
            exit 1
          fi

      - name: Check out repository tree
        uses: actions/checkout@v5

      - name: Validate every tracked path
        shell: bash
        run: python3 ci/validate-pathnames.py HEAD
